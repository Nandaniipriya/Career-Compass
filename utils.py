import streamlit as st

def load_css():
    """
    Apply custom CSS to streamlit app.
    Note: Limited custom CSS to maintain Streamlit's standard look as per guidelines.
    """
    # Since we're instructed not to use custom CSS unless explicitly requested,
    # we'll just keep this function minimal and use Streamlit's default styling.
    pass
